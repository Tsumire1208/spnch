div class="chats-header">
  <div class="chats-left-header">
      <div class="chats-header-title">
        <%# <strong><%= @room.name %></strong> %>
      </div>
  </div>
    <div class="chats-right-header">
      <div class="chats-header-button">
        <%# <%= button_to "チャットを終了する", room_path(@room), method: :delete %> %>
      </div>
    </div>
</div>

<div class="messages">
<%# <%= render partial: 'chat', collection: @chats %> %>
</div>

<%# <%= form_with model: [@room, @chat], class: 'form', local: true do |f| %> %>
  <div class="form-input">
    <%# <%= f.text_field :content, class: 'form-message', placeholder: 'type a message' %> %>
    <label class="form-image">
      <span class="image-file">画像</span>
      <%# <%= f.file_field :image, class: 'hidden' %> %>
    </label>
  </div>
  <%# <%= f.submit '送信', class: 'form-submit' %> %>
<%# <% end %> %>
